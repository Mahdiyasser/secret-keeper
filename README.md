# secret-keeper
This browser app safely stores your sensitive data using CryptoJS. All encryption/decryption is performed locally in the browser, secured by a master key. Data is stored exclusively in local storage, ensuring max privacy and offline access. Features: saving credentials, contacts, notes, secure links and vault export/import for backups.
